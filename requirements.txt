# ============================================================
# 📦 REQUIREMENTS.TXT - List of All Libraries Your App Needs
# ============================================================
# 
# All versions updated to be compatible with NumPy 2.x
# (because your model was trained with NumPy 2.x)
# ============================================================

# ----- WEB FRAMEWORK -----
flask==2.3.3
werkzeug==2.3.7
jinja2==3.1.2

# ----- DATA SCIENCE LIBRARIES -----
# NumPy 2.x (your model was trained with this)
numpy>=2.0.0

# Pandas 2.2+ (compatible with NumPy 2.x)
pandas>=2.2.0

# Scikit-learn 1.4+ (compatible with NumPy 2.x)
scikit-learn>=1.4.0

# ----- ML MODEL LIBRARIES -----
catboost>=1.2
xgboost>=2.0.0

# ----- UTILITY LIBRARIES -----
dill>=0.3.7

# Visualization (optional for production)
seaborn>=0.13.0
matplotlib>=3.8.0
