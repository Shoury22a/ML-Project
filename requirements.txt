# ============================================================
# 📦 REQUIREMENTS.TXT - List of All Libraries Your App Needs
# ============================================================
# 
# WHAT IS THIS FILE?
# -------------------
# This file lists every Python library your app needs to run.
# When Docker runs "pip install -r requirements.txt", it 
# installs EXACTLY these libraries.
#
# FORMAT: library_name==version_number
# The == pins to a specific version so it works the same everywhere!
# ============================================================

# ----- WEB FRAMEWORK -----
# Flask = The web framework that runs your website
flask==2.3.3

# Werkzeug = Flask's helper for handling web requests
werkzeug==2.3.7

# Jinja2 = Template engine (renders your HTML files)
jinja2==3.1.2

# ----- DATA SCIENCE LIBRARIES -----
# Pandas = For working with data tables (DataFrames)
pandas==2.0.3

# NumPy = For mathematical operations and arrays
numpy==1.24.3

# Scikit-learn = Machine learning library (has StandardScaler, etc.)
scikit-learn==1.3.0

# ----- ML MODEL LIBRARIES -----
# CatBoost = Gradient boosting ML library (your model might use this)
catboost==1.2

# XGBoost = Another gradient boosting library
xgboost==1.7.6

# ----- UTILITY LIBRARIES -----
# Dill = For saving/loading Python objects (like your model.pkl)
dill==0.3.7

# Seaborn = For data visualization (may not be needed in production)
seaborn==0.12.2

# Matplotlib = For plotting graphs
matplotlib==3.7.2
